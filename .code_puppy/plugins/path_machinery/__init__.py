"""path_machinery — sync Code Puppy's subprocess PATH with the user's real
login-shell PATH (~/.zprofile, ~/.zshrc, ~/src/tools/shellrc/* — the same
machinery Wibey's continuous shell and every interactive terminal inherit).

See register_callbacks.py for the why/how.
"""
